# 模型-代码追溯矩阵

> 本矩阵将 C0 模型的理论要素 / 方程映射到 `simulations/vector-thrust-lab/src/core/` 中的具体实现位置、单元测试及回归基线场景。用于回答「模型中的某条方程在代码哪里被实现、被什么测试覆盖」。

---

| 模型要素 / 方程 | 代码位置 `src/core/<file>.mjs` | 单元测试 | 回归基线场景 |
|-----------------|-------------------------------|----------|--------------|
| **四元数姿态积分** `q̇ = ½·q⊗ω_body`（右乘） | `math.mjs:quatMultiply`（30–37）、`math.mjs:quatNormalize`（39–43）；`dynamics.mjs:physicsStep`（60–66） | `math.test.mjs`：乘法单位元、逆元、归一化；`dynamics.test.mjs`：长时间积分保持归一化、子步调度等价 | `trim`、`pitchStep`、`rollStep` |
| **欧拉角提取** 3-2-1 约定 `theta = -asin(R13)` | `math.mjs:eulerFromQuat`（48–64） | `math.test.mjs`：纯俯仰四元数 → `theta = -a0`；重力变换方向一致性 | `trim`、`pitchStep`、`rollStep` |
| **重力变换到机体系** `g_body = R⁻¹·g_ned` | `math.mjs:rotateVecByQuat`（12–24）、`math.mjs:quatInvert`（27）；`dynamics.mjs:physicsStep`（35） | `math.test.mjs`：旋转再逆旋转还原；重力变换方向 | `trim` |
| **SAS 三通道反馈** 角速率阻尼 + 姿态比例 + 积分，输出限幅 | `control.mjs:applySas`（9–22） | `control.test.mjs`：直通、俯仰 / 偏航 / 滚转极性、执行限幅、积分限幅 | `trim`、`pitchStep`、`rollStep` |
| **差速分配** `ω_f = ω_0·sqrt(1+Δω)`，`ω_t = ω_0·sqrt(1-Δω)`，平方和近似不变 | `propulsion.mjs:stepPropulsion`（11–14） | `propulsion.test.mjs`：平方转速和不变 | `trim`、`rollStep` |
| **电机一阶滞后** `ω_{k+1} += (ω_t − ω_k)·min(dt/τ_M, 1)` | `propulsion.mjs:stepPropulsion`（15–16） | `propulsion.test.mjs`：一阶滞后指数趋近 | `trim`、`pitchStep`、`rollStep` |
| **推力 / 反扭模型** `T = kT·ω²`、`τ = kQ·ω²`、`τ_rot = J_p·ω̇` | `propulsion.mjs:stepPropulsion`（21–23） | `propulsion.test.mjs`：稳态 T/Q 关系 | `trim`、`pitchStep`、`rollStep` |
| **推力矢量六维映射** `Fx/Fy/Fz`、`Mx/My/Mz` 由摆角与转速生成 | `propulsion.mjs:stepPropulsion`（27–33） | `propulsion.test.mjs`：前摆符号、尾摆符号、零差速 `Mx≈0` | `trim`、`pitchStep`、`rollStep` |
| **线性气动 5 项** 开关、迎角升力、阻尼、侧滑、空速下限 | `aerodynamics.mjs:computeAero`（9–33） | `aerodynamics.test.mjs`：开关归零、正迎角升力、俯仰阻尼、侧滑、低速奇异 | `trim`、`pitchStep`、`rollStep` |
| **子步调度** `n = ceil(dt/maxStep)`、`h = dt/n` | `dynamics.mjs:stepPhysics`（12–16） | `dynamics.test.mjs`：子步调度与手动子步等价 | `trim`、`pitchStep`、`rollStep` |
| **地面约束** `pos.z > groundZ && vWorld.z > 0` 时钳位 | `dynamics.mjs:physicsStep`（74–78） | `dynamics.test.mjs`：触地清零下降速度、上升不被拦截 | `trim`、`rollStep` 标称未触发；`pitchStep` 从 `z=-100 m` 起飞以隔离地面事件 |
| **配平初始化** `a0 = aTrim`、`v0 = vTrim`、`θ0 = a0`、平飞航迹 | `state.mjs:createSimulationState`（6–38）、`state.mjs:resetFlightState`（41–50） | `dynamics.test.mjs`：配平直飞 10 s 有界 | `trim` |
| **遥测快照** 供 HUD / 浏览器层读取的只读视图 | `telemetry.mjs:getTelemetry`（5–24） | 间接通过浏览器 E2E 检查（VER-001 §2.4） | `trim`、`pitchStep`、`rollStep` |
| **参数单一事实源** 52 参数冻结对象 | `parameters.mjs:P` | `parameters.test.mjs`：52 键完整、正值、符号、限幅、冻结、配平角换算 | 所有场景的输入真值源 |

---

## 使用说明

- 表格中的行号对应 `simulations/vector-thrust-lab/src/core/` 下各文件在 C0 基线中的位置；若代码重构导致行号变化，应同步更新本矩阵。
- 「回归基线场景」为 `tests/scenarios.mjs` 中定义的 `trim`、`pitchStep`、`rollStep`；各场景通过 `tests/regression.test.mjs` 与 `tests/fixtures/regression-baseline.json` 比对。
- 若新增模型要素，须在本矩阵中新增一行，并补充对应单元测试或演示检查证据。
