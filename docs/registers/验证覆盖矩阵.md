# 验证覆盖矩阵

> 本矩阵将 [REQ-001](../02-需求与验证/REQ-001-需求基线.md) 中的每条需求映射到验证方法（I/A/T/D）、具体测试或证据，以及当前满足状态。它是 VER-001 中 V 模型右升路径的详细展开。

---

## 1 功能需求

| REQ-ID | 标题 | 验证方法 | 测试 / 证据 | 状态 |
|--------|------|----------|-------------|------|
| REQ-SIM-001 | 六自由度刚体动力学积分（平动、转动、四元数姿态） | T, A | `src/core/dynamics.mjs`；`tests/dynamics.test.mjs`（四元数归一化、配平直飞有界、无阻尼发散） | 已满足 |
| REQ-SIM-002 | 积分子步调度，单步长不超过 `maxStep = 0.004 s` | T, A | `dynamics.mjs:stepPhysics`；`tests/dynamics.test.mjs`「子步调度」 | 已满足 |
| REQ-SIM-003 | 地面约束平面（NED z = `groundZ = 6.2 m`）防止穿透 | T | `dynamics.mjs:physicsStep`（74–78）；`tests/dynamics.test.mjs`地面约束测试 | 已满足 |
| REQ-CTL-001 | 三通道直接映射：前摆 → 偏航、尾摆 → 俯仰、差速 → 滚转 | T, D | `src/core/propulsion.mjs`；`tests/propulsion.test.mjs`符号测试；页面方程面板 | 已满足 |
| REQ-CTL-002 | SAS 三通道角速率 / 姿态角 / 积分反馈，输出限幅 | T | `src/core/control.mjs`；`tests/control.test.mjs` | 已满足 |
| REQ-CTL-003 | 执行机构速率 / 带宽限幅模型 | A | 当前为理想执行机构；见 [CFG-000](../01-方案设计/CFG-000-概念构型基线C0.md) §6 | 未满足 |
| REQ-PRP-001 | 差速开方分配：`ωf = ω0·sqrt(1+Δω)`，`ωt = ω0·sqrt(1-Δω)` | T, A | `propulsion.mjs:stepPropulsion`（11–14）；`tests/propulsion.test.mjs`平方和不变 | 已满足 |
| REQ-PRP-002 | 电机一阶滞后模型，时间常数 `tauM = 0.28 s` | T | `propulsion.mjs:stepPropulsion`（15–16）；`tests/propulsion.test.mjs`一阶滞后 | 已满足 |
| REQ-PRP-003 | 零差速等转速时反扭矩相互抵消（`Mx ≈ 0`） | T | `propulsion.mjs:stepPropulsion`（31）；`tests/propulsion.test.mjs`零差速反扭对消 | 已满足 |
| REQ-AER-001 | 线性小迎角气动模型，空速下限 `vMin = 0.5 m/s` | T | `src/core/aerodynamics.mjs`；`tests/aerodynamics.test.mjs` | 已满足 |
| REQ-PAR-001 | 参数单一事实源 `models/aircraft-model.json`，支持漂移检测 | I, T | `tools/sync-params.py --check`；`src/core/parameters.mjs`；[`参数数据手册`](./参数数据手册.md) | 已满足 |
| REQ-TST-001 | 单元测试覆盖核心模块 | T | `tests/*.test.mjs`，约 40 项断言；`node --test tests/` | 已满足 |
| REQ-TST-002 | 回归基线比对（配平直飞 / 俯仰阶跃 / 滚转阶跃） | T | `tests/regression.test.mjs`；`tests/fixtures/regression-baseline.json` | 已满足 |
| REQ-UI-001 | 四通道操纵输入滑块 | D, I | `index.html`；`src/browser/controls-ui.mjs` | 已满足 |
| REQ-UI-002 | HUD 遥测面板显示推力、反扭、空速、迎角、姿态、气动力矩 | D, I | `index.html`；`src/browser/hud.mjs`；`src/core/telemetry.mjs` | 已满足 |
| REQ-UI-003 | 演示序列：俯仰 / 偏航 / 滚转 / 综合，自动启动综合演示 | D | `src/browser/demo.mjs`；`src/main.js` | 已满足 |
| REQ-UI-004 | 复位、SAS 开关、气动力开关功能 | D, T | `src/browser/controls-ui.mjs`；`tests/control.test.mjs` | 已满足 |
| REQ-UI-005 | 文档中心（模型方程模态框）与主题切换 | D, I | `index.html`；`src/browser/theme.mjs` | 已满足 |

## 2 性能与约束需求

| REQ-ID | 标题 | 验证方法 | 测试 / 证据 | 状态 |
|--------|------|----------|-------------|------|
| REQ-PERF-001 | 单帧积分时间受 `frameCap = 0.05 s` 限制 | I, D | `src/main.js`；`dynamics.mjs:stepPhysics` | 已满足 |
| REQ-PERF-002 | 无构建静态部署：纯 HTML/ESM/原生 JS | I | 项目目录结构；`index.html` 直接由 HTTP 服务 | 已满足 |
| REQ-PERF-003 | 零依赖测试：仅使用 Node.js 内置 `node:test` | I, T | `tests/*.test.mjs`；运行命令 `node --test tests/` | 已满足 |
| REQ-PERF-004 | 提供 Windows 批处理 `启动.bat` | I, D | `simulations/vector-thrust-lab/启动.bat` | 已满足 |

## 3 接口需求

| REQ-ID | 标题 | 验证方法 | 测试 / 证据 | 状态 |
|--------|------|----------|-------------|------|
| REQ-IFC-001 | `models/aircraft-model.json` 须符合 JSON Schema | I, T | `models/aircraft-model.schema.json`；`tools/sync-params.py` 解析校验 | 已满足 |
| REQ-IFC-002 | `src/core/parameters.mjs` 由生成脚本产出，禁止手改；`--check` 可检测漂移 | I, T | 生成文件头部注释；`sync-params.py --check` | 已满足 |
| REQ-IFC-003 | 页面 DOM id 契约保持向后兼容 | I | `index.html`；`src/browser/controls-ui.mjs`；`src/browser/hud.mjs` | 已满足 |

---

## 4 状态汇总

| 状态 | 数量 | 说明 |
|------|------|------|
| 已满足 | 24 | 功能、性能、接口、测试需求已有代码 / 测试 / 检查证据 |
| 部分满足 | 0 | — |
| 未满足 | 1 | REQ-CTL-003 执行机构速率 / 带宽限幅，作为已知限制登记于 [`假设与局限日志`](./假设与局限日志.md) ASM-002 |

---

## 5 维护规则

- 新增需求时，须在本矩阵中新增一行，并指定验证方法与证据。
- 验证方法变化或测试文件重命名时，同步更新本矩阵。
- 未满足需求转为已满足时，须经代码 / 测试审查，并更新 [REQ-001](../02-需求与验证/REQ-001-需求基线.md)。
