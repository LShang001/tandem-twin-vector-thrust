# 工况与试验卡（模板）

> 本卡用于记录「纵列双发矢量推力飞行器」C0 仿真项目的每一次仿真试验或验证运行。每次试验前填写本卡，试验后记录结果与结论，并关联需求、假设与失效模式。

---

## 1 基本信息

| 项目 | 内容 |
|------|------|
| 工况 ID | `CAS-YYYY-MM-DD-NNN`（示例：CAS-2026-07-20-001） |
| 日期 | YYYY-MM-DD |
| 试验目的 | 简述本次试验要验证或观察的内容 |
| 试验人员 | 姓名 / 角色 |
| 模型版本 | Git commit hash：`__________________` |
| 参数版本 | `models/aircraft-model.json` 版本 / 状态：`__________________` |
| 仿真代码版本 | `simulations/vector-thrust-lab/src/core/` 对应 commit：`__________________` |
| 回归基线版本 | `tests/fixtures/regression-baseline.json` 对应 commit：`__________________` |

## 2 初始条件

| 状态量 | 值 | 单位 | 备注 |
|--------|-----|------|------|
| 位置 `pos.x / y / z` | 0 / 0 / 0 | m | NED 系，z 向下 |
| 机体系速度 `vel.x / y / z` | `vTrim·cos(aTrim)` / 0 / `vTrim·sin(aTrim)` | m/s | 默认配平；自定义时填写 |
| 姿态四元数 `quat` | `[0, sin(aTrim/2), 0, cos(aTrim/2)]` | - | 默认配平平飞 |
| 角速度 `omega.x / y / z` | 0 / 0 / 0 | rad/s | |
| 油门 `thr` | 0.5 | - | 默认配平油门 |
| 前摆指令 `df` | 0 | rad | |
| 尾摆指令 `dt` | 0 | rad | |
| 差速指令 `dw` | 0 | - | |
| SAS 开关 | true / false | - | |
| 气动开关 | true / false | - | |

## 3 输入序列

| 时间段（s） | 帧索引范围 | 通道 | 值 | 说明 |
|-------------|------------|------|-----|------|
| 0.0 – 4.0 | 0 – 239 | `dt` | `8 * π / 180` | 俯仰阶跃示例 |
| 4.0 – 8.0 | 240 – 479 | `dt` | 0 | 撤除输入 |
| | | | | |
| | | | | |

## 4 通过判据

| 序号 | 判据 | 是否通过 | 备注 |
|------|------|----------|------|
| 1 | 仿真全程无 Console 报错 | | |
| 2 | 姿态 / 速度 / 位置数值有界（不发散） | | |
| 3 | 特定时刻姿态角满足预期符号 / 范围 | | |
| 4 | 与回归基线差异在容差内（若适用） | | |
| 5 | 与手工解析或参考解一致（若适用） | | |

## 5 结果记录

### 5.1 关键遥测曲线 / 快照

| 时间（s） | V（m/s） | α（rad） | φ（rad） | θ（rad） | ψ（rad） | p / q / r（rad/s） | 备注 |
|-----------|----------|----------|----------|----------|----------|--------------------|------|
| | | | | | | | |
| | | | | | | | |

### 5.2 观察到的异常或现象

- 
- 

### 5.3 日志 / 截图 / 基线差异

- 截图路径：`__________________`
- Console 日志路径：`__________________`
- 与回归基线差异摘要：`__________________`

## 6 结论

| 项目 | 内容 |
|------|------|
| 结论 | 通过 / 不通过 / 部分通过 |
| 主要发现 | 简述试验验证或暴露的问题 |
| 后续行动 | 是否需要更新代码、测试、文档或基线 |
| 签字 | 试验人员 / 审查人员 |

## 7 关联追踪

| 类型 | ID |
|------|-----|
| 关联需求 | REQ-_____ |
| 关联假设 / 局限 | ASM-_____ |
| 关联失效模式 / 危害 | HAZ-_____ |
| 关联文档 | __________________ |
| 关联回归场景 | `trim` / `pitchStep` / `rollStep` / 自定义 |

---

## 使用说明

1. 每次新增试验前复制本模板，按顺序编号并填写完整。
2. 「模型版本」「参数版本」「回归基线版本」应至少记录 Git commit hash，确保可追溯。
3. 输入序列应尽量覆盖阶跃、脉冲、扫频、组合机动等典型模式；必要时引用 `tests/scenarios.mjs` 中的已有场景。
4. 试验结论为「不通过」或「部分通过」时，必须填写后续行动并关联到具体需求、假设或危害。
